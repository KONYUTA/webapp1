<% if @menu.errors.any? %>
 <%= pluralize(@menu.errors.count, "error") %>  prohibited this obj from being saved:
  <ul>
   <% @menu.errors.full_messages. each do |message| %>
    <li><%= message %></li>
   <% end %>
  </ul>
 <% end %>
<h1>投稿</h1>
<%= form_with model: @menu, local:true do |f| %>
   <h4>Title</h4>
   <%= f.text_field :title %>
   <h4>Opinion</h4>
   <%= f.text_area :caption %>
       <%= f.label"タグ (,で区切ると複数タグ登録できます)" %>
    <%= f.text_field :tag_name%>

    <%= f.submit "投稿",class:"btn btn-outline-primary btn-block" %>
<% end %>